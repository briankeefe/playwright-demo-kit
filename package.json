{
  "name": "playwright-demo-kit",
  "version": "0.1.0",
  "private": false,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "demo:test": "playwright test -c playwright.demo.config.ts",
    "demo:render": "node scripts/render-demo-video.mjs",
    "demo:flow": "npm run demo:test && npm run demo:render"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
